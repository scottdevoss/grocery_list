<%= form_with url: "/update/#{@items.id}", method: :patch, local: :true do |form|  %>
  <%= form.label :name, "Item:" %>
  <%= form.text_field :name, value: "#{@items.name}"  %>
  <%= form.submit "Update" %>
<% end %>